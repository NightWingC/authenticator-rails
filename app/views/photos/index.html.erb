<h1>Photos</h1>
<%= content_for :sidebar_options do %>
    <p>Hola desde index/photos</p>
<% end %>
<% @photos.each do |photo| %>
    <div>
        <h2><%= photo.title %></h2>
        <%# <%= image_tag photo.image_url %>
        <img src="<%= photo.image_url %>">
        <%= link_to "Ver más", photo %>
    </div>
<% end %>
